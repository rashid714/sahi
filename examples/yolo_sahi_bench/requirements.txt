# App dependencies (CPU-first). Keep versions loose for easier deploy.
streamlit>=1.30
ultralytics>=8.0
sahi>=0.11
numpy>=1.24
pillow>=10.0
pyyaml>=6.0

# Video / image IO (use headless for servers)
opencv-python-headless>=4.8

# ML backend
torch>=2.0
